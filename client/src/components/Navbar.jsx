// Step 17 (BUILD-STEPS.md): real navbar (auth state + cart badge) gets built here
function Navbar() {
  return (
    <nav className="p-4 bg-white shadow-sm">
      <span className="font-semibold">MERN Store</span>
    </nav>
  )
}

export default Navbar
